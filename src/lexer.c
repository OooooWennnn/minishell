t_token *tokenize(char *input) {
    t_token *head = NULL;
    t_token *curr = NULL;
    int i = 0;
    
    while (input[i] != '\0') {
        if (input[i] == ' ' || input[i] == '\t') {
            
        }
        
    }
} 